version: "3.6"
services:

  nginx:
    build:
      dockerfile: Dockerfile
      context: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf

  app:
    build:
      dockerfile: Dockerfile
      context: app
    ports:
      - "8000:8000"
